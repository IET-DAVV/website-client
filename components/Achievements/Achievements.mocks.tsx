import { AchievementProps } from '.';

const base: AchievementProps = {
  sampleProp: 'sampleProp 1',
};

const alt: AchievementProps = {
  sampleProp: 'sampleProp 2',
};

export default {
  base,
  alt,
};
