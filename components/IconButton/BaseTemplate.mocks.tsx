import { ButtonWithTextAndIconProps, IconButtonProps } from '.';

const base: ButtonWithTextAndIconProps = {
  sampleProp: 'sampleProp 1',
};

const alt: IconButtonProps = {
  sampleProp: 'sampleProp 2',
};

export const mockBaseTemplateProps = {
  base,
  alt,
};
